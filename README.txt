README

For easiest compilation, run Live Server with the stopwatch.html file in VSCode or another platform.

In the console window, type sw.reset() to reset stopwatch, sw.start() to start it, and sw.stop() to pause it.

